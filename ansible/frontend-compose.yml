services:
  frontend:
    build:
      context: /home/ubuntu/app/frontend
      dockerfile: Dockerfile
      args:
          VITE_BACKEND_ENDPOINT: http://private-lb-fp-3a5e927ca6890b41.elb.us-east-1.amazonaws.com:5000
          VITE_SERVER_HOSTNAME: ${HOSTNAME}
    ports:
      - 3000:3000
